# Optional — without it the pipeline runs in captions mode (on-screen text, no narration).
ELEVENLABS_API_KEY=

# Login the capture scripts may use (referenced per-walkthrough in videos/<slug>/capture.ts).
CAPTURE_EMAIL=
CAPTURE_PASSWORD=
